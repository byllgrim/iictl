package body ChCtl is
    procedure Rejoin_Channels is
    begin
        null;
        -- TODO for server in irc dir
            -- TODO for channel in server
                -- TODO rejoin if fifo "in" is down?
    end Rejoin_Channels;
end ChCtl;
